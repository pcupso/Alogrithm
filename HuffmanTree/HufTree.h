#ifndef HUFF_TREE_H_H
#define HUFF_TREE_H_H

#define MAX_TREE_COUNT	50

#include <vector>


class HufTree
{
public:
	HufTree(void);
	~HufTree(void);

	void InitializeTree(void);
	//void SortTree(struct HufTreeNode nodes[], int nCount);

};

#endif